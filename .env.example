# Environment variables example
# Copy this file to .env.local for development or .env.production for production builds

# API base URL - change this to point to your API server
VITE_API_BASE_URL=http://localhost:5309

# Development host (optional, for HMR) (not needed for production)
DEV_HOST=localhost